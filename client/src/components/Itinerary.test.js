import React from "react";
import { shallow } from "enzyme";
import { Itinerary } from "./Itinerary";

// assertions for favouriting an itinerary
// it("clicking favourite button should call method onClickAdd")
// it("favourite list should be updated")
// it("favourite button should change from empty to filled heart when itinerary is a favourite")
